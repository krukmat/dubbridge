appId: com.dubbridge.mobile
name: T7local real login
tags:
  - android
  - s-230
  - t7local
---
- tapOn:
    id: login-email-input
- setClipboard: ${T7LOCAL_EMAIL}
- pasteText

- tapOn:
    id: login-password-input
- setClipboard: ${T7LOCAL_PASSWORD}
- pasteText

# Clipboard paste updates React state asynchronously. Wait until the rendered
# submit control reflects the completed password value before sending IME Done.
- extendedWaitUntil:
    visible:
      id: login-submit-button
      enabled: true
    timeout: 5000

# Submit through the password field's native IME action instead of depending on
# a first post-keyboard tap, which Android consumes in this emulator path.
- pressKey: Enter

- extendedWaitUntil:
    visible:
      id: home-screen
    timeout: 20000

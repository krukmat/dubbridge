---
type: Audit
title: "S-230 T6p-a RRI"
status: complete
slice: S-230
task: S-230-T6p-a
date: 2026-09-26
---

# S-230-T6p-a — RRI recomputation

The current `scripts/rri.py` / ADR-045 v2 formula was recomputed for the
approved T6p-a freeze envelope.

## Inputs

- C = 0 — non-development planning/config freeze; no executable control flow changed.
- F = 2 — eight documentation/status paths are in the T6p-a allowed-path set;
  the script maps 3–5 files to F=2, while this task's eight paths are status/evidence
  synchronization rather than eight independent runtime modules. To stay fail-closed
  for the actual path count, the execution envelope is decomposed into the already
  owner-reviewed a.1–a.8 substeps and no source implementation is performed here.
- D = 3 — integration/deployment state and lifecycle ownership.
- T = 0 — no executable behavior is changed by this freeze.
- A = 0 — objective, exclusions, ordered subtasks, and closure criteria are explicit.
- K = 3 — production Compose, worker publication runtime, Availability Node,
  storage, mTLS, and secret injection must remain mutually consistent.
- P = 4 — secret/identity/ownership exposure is frozen.
- X = 3 — multiple production/runtime surfaces must be inspected together.
- manual penalties: `auth_security +10`, `arch_decision +12`.

## v2 result

Technical profile:

- L = 0
- I = 3
- Q = 3
- V = 0
- bottleneck B = 3
- ICI = 75
- ICI band input = 70

Risk/domain input:

- weighted D/P/K base = 24
- penalties = 22
- risk band input = 46

**Final RRI = max(70, 46) = 70 — Complex / Effort L.**

The RRI >= 56 decomposition trigger applies. The task was therefore
decomposed before execution into T6p-a.1 through T6p-a.8 and the owner
explicitly approved execution of that plan in the conversation on
2026-09-26.

Because T6p-a is planning/config/status-only, the workflow's task-analysis
review and code-solution review exemptions apply:

- Task-analysis review: n/a — planning/config/task-ledger-only exemption.
- Code-solution review: n/a — no product/source implementation.

No local/cloud developer delegation is used for this block.

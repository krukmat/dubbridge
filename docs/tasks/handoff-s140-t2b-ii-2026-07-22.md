---
type: TaskList
status: done
---

# Handoff: S-140-T2b-ii follow-up (worker-runner preparation refactor)

Sesión iniciada en Codex. Este handoff deja a Claude el estado exacto del
refactor agresivo pedido por el usuario y el hueco real que queda por cerrar.

## Contexto

Task principal: `docs/tasks/s-140-subtitle-generation.md`, sección
`## S-140-T2b-ii: Transcription-ready subtitle enqueue hook`.

Follow-up ya aprobado y ejecutado en esta sesión:

- Task ID: `S-140-T2b-ii-followup`
- Objetivo: bajar tamaño y complejidad cognitiva del worker runner, con foco en
  `preparation_runtime.rs`, sin cambiar comportamiento.
- RRI: `40`
- Banda: `Moderate`
- Phase 1:
  `Task-analysis review: gemma .agent/peer-task-review-S-140-T2b-ii-prep-runtime-slim.json - PASS`

Importante: el usuario no quería solo “cumplir la task”; pidió explícitamente
refactorización agresiva, bajar CC y mejorar diseño.

## Qué ya quedó hecho

Se partió el runtime y se extrajeron módulos reales:

- `apps/worker-runner/src/preparation_runtime.rs`
- `apps/worker-runner/src/preparation_artifact_persistence.rs`
- `apps/worker-runner/src/preparation_media_executor.rs`
- `apps/worker-runner/src/transcription_runtime.rs`
- `apps/worker-runner/src/subtitle_enqueue.rs`

También se movieron los tests inline a un árbol dedicado:

- `apps/worker-runner/src/preparation_runtime_tests.rs`
- `apps/worker-runner/src/preparation_runtime_tests/support.rs`
- `apps/worker-runner/src/preparation_runtime_tests/preparation_flow.rs`
- `apps/worker-runner/src/preparation_runtime_tests/enqueue_flow.rs`

`preparation_runtime_tests.rs` quedó en **3 líneas** porque ahora solo es el
entrypoint del árbol de tests:

```rust
mod enqueue_flow;
mod preparation_flow;
mod support;
```

No “desaparecieron” los tests: el cuerpo fue movido a los submódulos.

## Tamaños actuales

Conteo medido en esta sesión:

- `apps/worker-runner/src/preparation_runtime.rs` — `144`
- `apps/worker-runner/src/preparation_artifact_persistence.rs` — `116`
- `apps/worker-runner/src/preparation_media_executor.rs` — `152`
- `apps/worker-runner/src/preparation_runtime_tests.rs` — `3`
- `apps/worker-runner/src/preparation_runtime_tests/support.rs` — `120`
- `apps/worker-runner/src/preparation_runtime_tests/preparation_flow.rs` — `244`
- `apps/worker-runner/src/preparation_runtime_tests/enqueue_flow.rs` — `257`
- `apps/worker-runner/src/main.rs` — `48`

Total del árbol de tests de preparación tras el split: `624` líneas.

## Estado funcional verificado

Checks ya corridos y verdes:

- `cargo check -p dubbridge-worker-runner`
- `cargo test -p dubbridge-worker-runner`

Resultado observado: `21` tests passed.

Phase 2 ya registrada para el refactor principal:

- `Code-solution review: gemma .agent/peer-code-review-S-140-T2b-ii-followup.json - PASS`

Phase 2 ya registrada para el split adicional de tests:

- `Code-solution review: gemma .agent/peer-code-review-S-140-T2b-ii-tests-slim.json - PASS`

Nota de proceso: `qwen3.6:27b-q4_K_M` hizo timeout y el fallback útil terminó
siendo `gemma4:26b-a4b-it-qat`.

## Qué sí está cubierto hoy

La cobertura quedó distribuida entre:

- `apps/worker-runner/src/preparation_runtime_tests/preparation_flow.rs`
- `apps/worker-runner/src/preparation_runtime_tests/enqueue_flow.rs`
- tests propios en `apps/worker-runner/src/subtitle_enqueue.rs`
- tests propios en `apps/worker-runner/src/transcription_runtime.rs`

Esto cubre bien:

- transición de preparación a `Ready`
- persistencia de probe + HLS
- fail-closed cuando falla HLS
- validación de salida HLS inválida
- rechazo de envelopes de tipo incorrecto
- handoff hacia transcription queue
- manejo de falta de `target_languages`
- no duplicar enqueue post-ready

## Hueco real pendiente

El hueco importante ya detectado es este:

- `apps/worker-runner/src/preparation_media_executor.rs` **no tiene cobertura
  unitaria real**.

Razón:

- Los tests de preparación usan `FakePreparationExecutor`, así que validan la
  orquestación pero no ejercitan el executor subprocess real ni sus helpers.

Eso deja sin probar directamente:

- `run_command`
- `read_hls_segments`
- `path_to_string`
- y, según cómo se enfoque, parte del flujo de `extract_probe_metadata` /
  `transcode_hls` en el executor real

`main.rs` tampoco tiene tests propios, pero ahí el riesgo es bajo porque ya es
solo wiring liviano.

## Recomendación de continuación

Si Claude sigue “esa parte”, el siguiente paso valioso no es tocar el archivo de
3 líneas, sino cerrar la cobertura del executor real y, si hace falta, seguir
mejorando cohesión interna.

Orden sugerido:

1. Revisar `apps/worker-runner/src/preparation_media_executor.rs`.
2. Extraer helpers puros o seams chicos si eso hace los tests más simples.
3. Agregar tests unitarios/aislados para:
   - `run_command` éxito y error
   - `read_hls_segments` filtra/extensión/orden/carga bytes
   - `path_to_string` falla cerrado con path no UTF-8 si el entorno lo permite
4. Solo si el módulo sigue sintiéndose denso, hacer un segundo split pequeño
   con nombres que refuercen responsabilidad.
5. Revalidar con:
   - `cargo check -p dubbridge-worker-runner`
   - `cargo test -p dubbridge-worker-runner`

## Restricciones importantes

- No revertir cambios ajenos del worktree.
- Usar `apply_patch` para ediciones manuales.
- Mantener comportamiento existente; este follow-up es de diseño, cohesión y
  testabilidad, no de features nuevas.
- Si se amplía el alcance más allá de este hueco acotado, reevaluar si sigue
  siendo continuación del follow-up ya aprobado o si conviene abrir task nueva.

## Prompt listo para Claude

```md
Continúa el follow-up de `S-140-T2b-ii` en `/Users/matias/dubbridge`.

Contexto ya resuelto:

- `preparation_runtime.rs` fue refactorizado agresivamente y quedó en 144 líneas.
- Se extrajeron:
  - `apps/worker-runner/src/preparation_artifact_persistence.rs` (116)
  - `apps/worker-runner/src/preparation_media_executor.rs` (152)
- Los tests inline salieron a:
  - `apps/worker-runner/src/preparation_runtime_tests.rs` (3)
  - `apps/worker-runner/src/preparation_runtime_tests/support.rs` (120)
  - `apps/worker-runner/src/preparation_runtime_tests/preparation_flow.rs` (244)
  - `apps/worker-runner/src/preparation_runtime_tests/enqueue_flow.rs` (257)
- `main.rs` quedó en 48 líneas.
- `cargo check -p dubbridge-worker-runner` y `cargo test -p dubbridge-worker-runner` ya pasaron.
- Review artifacts vigentes:
  - `Task-analysis review: gemma .agent/peer-task-review-S-140-T2b-ii-prep-runtime-slim.json - PASS`
  - `Code-solution review: gemma .agent/peer-code-review-S-140-T2b-ii-followup.json - PASS`
  - `Code-solution review: gemma .agent/peer-code-review-S-140-T2b-ii-tests-slim.json - PASS`

El hueco real pendiente es que `apps/worker-runner/src/preparation_media_executor.rs`
no tiene cobertura unitaria directa. Los tests actuales usan `FakePreparationExecutor`,
así que validan la orquestación pero no el executor subprocess real.

Quiero que te enfoques en esa parte:

1. Revisa `apps/worker-runner/src/preparation_media_executor.rs`.
2. Si ayuda a bajar complejidad o mejorar testabilidad, extrae helpers chicos y cohesionados.
3. Agrega tests directos para los helpers/seams del executor real, especialmente:
   - `run_command`
   - `read_hls_segments`
   - `path_to_string`
4. Mantén el comportamiento actual del worker-runner.
5. Revalida con:
   - `cargo check -p dubbridge-worker-runner`
   - `cargo test -p dubbridge-worker-runner`

No pierdas tiempo reexplicando por qué `preparation_runtime_tests.rs` tiene 3 líneas:
eso ya está entendido; es solo el entrypoint del árbol de tests.

Si detectas que el executor aún está demasiado acoplado, puedes hacer un split adicional
pequeño, pero solo si mejora claramente cohesión/CC y no reabre el scope completo.
```

## Cierre (2026-07-22, Claude)

El hueco quedó cerrado sin necesidad de un split adicional: el executor real ya
tenía cohesión suficiente, así que el trabajo fue agregar tests directos.

- Se agregó `#[cfg(test)] mod tests` a
  `apps/worker-runner/src/preparation_media_executor.rs` con 12 tests nuevos
  cubriendo `run_command` (éxito, exit no-cero, stderr en el mensaje de error,
  comando vacío, binario inexistente), `read_hls_segments` (filtro por
  extensión + orden, subdirectorios ignorados, directorio vacío, directorio
  faltante) y `path_to_string` (path UTF-8 válido, path no-UTF-8 en Unix).
  También se cubrió `write_source_workspace` (antes sin test alguno).
- Se removieron los `#[allow(dead_code)]` de las cuatro funciones ahora
  ejercitadas por tests; `cargo check` no generó warnings nuevos.
- No hizo falta split adicional: el módulo ya era pequeño (152 líneas) y
  cohesionado: cada función tenía una sola responsabilidad clara.

Verificación:

- `cargo fmt -p dubbridge-worker-runner -- --check` ✅
- `cargo clippy -p dubbridge-worker-runner --all-targets --all-features -- -D warnings` ✅
- `cargo check -p dubbridge-worker-runner` ✅
- `cargo test -p dubbridge-worker-runner` ✅ (33 passed, 0 failed — 21
  preexistentes + 12 nuevos)

No se tocó comportamiento existente; el cambio es puramente de cobertura de
tests sobre código ya presente.

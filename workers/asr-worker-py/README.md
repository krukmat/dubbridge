# ASR Worker Contract

This directory is reserved for the automatic speech recognition worker implementation.

The worker must accept JSON input that references authorized media artifacts and return transcript artifacts plus diagnostics through the schemas in this directory.

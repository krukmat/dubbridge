# TTS Worker Contract

This directory is reserved for text-to-speech and dubbing worker implementations.

The worker must operate only on consent-approved voice profiles and return segment plus merged-audio artifact references.

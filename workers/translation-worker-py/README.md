# Translation Worker Contract

This directory is reserved for the subtitle and text translation worker implementation.

The worker must preserve segment identity and produce deterministic outputs under the schemas defined here.
